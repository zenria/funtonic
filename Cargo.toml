[workspace]
resolver = "2"

members = [
    "common",
    "query-parser",
    "grpc-service",
    "executor",
    "commander",
    "taskserver",
    "integration",
    "exec",
]

[profile.release]
lto = "fat"
